---
layer: foundation
status: active
last_reviewed: 2026-05-26
epic: PW1
plan: ../plans/PLAN_pwa-standalone.md
---

# Чеклист плейтеста PW1 — resume после блокировки экрана

Внутренняя приёмка **фазы 0** (эпик PW1) перед включением сценария в [Pre-Alpha](PRE_ALPHA_PLAYTEST_PROTOCOL.md).

**Код:** `frontend-react/src/utils/appLifecycle.js`, `frontend-react/src/hooks/useGame.js` (`resyncAfterForeground`, `periodEndInFlightRef`).

**UX (TB1, май 2026):** на дашборде **нет** секундомера и play/pause — период **#N**, «Месяц открыт», CTA **«Закрыть месяц»**. Авто-закрытия по времени нет (`sync_time` → 0; `get_seconds_until_next` → 0). Сценарии ниже переписаны под TB1; старые шаги с «таймером» — в [приложении §A0](#a0-legacy-сценарии-с-таймером-архив).

---

## 0. Подготовка (5 мин)

| # | Действие | ✓ |
|---|----------|---|
| 0.1 | Зафиксировать **commit/tag** в таблице «Прогон» | |
| 0.2 | Стенд: prod/staging или локально `frontend` + `backend` | |
| 0.3 | Активная игра: `period_index` ≥ 1, на hero виден **#N** и **«Закрыть месяц»** | |
| 0.4 | **Минимум 2 прогона:** реальный телефон + Telegram TMA (обязательно); desktop — только вспомогательно | |

### Прогон (заполнять на каждое устройство)

| Поле | Прогон 1 (TMA) | Прогон 2 (TMA) | Прогон 0 (auto) |
|------|----------------|----------------|-----------------|
| Дата | | | 2026-05-26 |
| Тестер | | | agent |
| Commit (monorepo) | | | `b6a1052` |
| URL / билд | | | — |
| ОС / клиент TG | | | node `--test` |

---

## 0a. Автопроверка (до ручного TMA)

| Проверка | Команда | Ожидание | Результат |
|----------|---------|----------|-----------|
| A0.1 | `cd frontend-react && npm run test:utils` | exit 0, `appLifecycle.test.js: ok` | **PASS** 2026-05-26 |
| A0.2 | Код: `subscribeAppForeground` + debounce 400 ms в `useGame` | при `visible` → `GET` bootstrap | **PASS** (ревью) |
| A0.3 | Код: `periodEndInFlightRef` в `advancePeriod` | нет параллельных `setTimeNext` | **PASS** (ревью) |
| A0.4 | BE: `game_time.sync_time` | не двигает `period_index` | **PASS** (ревью) |

---

## 1. Сценарии (обязательные, TB1)

Отметить **PASS** / **FAIL** / **N/A** и одну строку заметок.

### A — Короткая блокировка (1–2 мин)

| Шаг | Действие | Ожидание | Прогон 1 | Прогон 2 |
|-----|----------|----------|----------|----------|
| A1 | Запомнить **#период** и **cash** на дашборде | — | | |
| A2 | Заблокировать экран **1–2 мин** или свернуть TG | — | | |
| A3 | Разблокировать, та же сессия TMA **без** перезапуска бота | ≤ ~2 с: hero показывает тот же **#N** (если месяц не закрывали) | | |
| A4 | Cash совпадает с «Финансы» / overview после resync | Нет «застывших» цифр | | |

### B — Длинная блокировка (3–5 мин), месяц не закрывали

| Шаг | Действие | Ожидение | Прогон 1 | Прогон 2 |
|-----|----------|----------|----------|----------|
| B1 | **Не** нажимать «Закрыть месяц» | — | | |
| B2 | Блокировка **3–5 мин** | — | | |
| B3 | Возврат | **#N** тот же; **нет** листа итогов периода; **нет** второго POST `time/next` | | |

### C — «Закрыть месяц» + фон (сеть в полёте)

| Шаг | Действие | Ожидание | Прогон 1 | Прогон 2 |
|-----|----------|----------|----------|----------|
| C1 | Нажать **«Закрыть месяц»** (подтвердить, если модалка) | — | | |
| C2 | Сразу свернуть TG / блокировка **до** ответа UI (1–3 мин) | — | | |
| C3 | Возврат | **Один** итог периода (sheet/tail) или корректный **#N+1**; нет двух sheet подряд | | |
| C4 | Нет белого экрана / вечного спиннера | UI отзывчив | | |

### D — Пауза в UI (TB1)

| Шаг | Действие | Ожидение | Прогон 1 | Прогон 2 |
|-----|----------|----------|----------|----------|
| D1 | Кнопки play/pause на дашборде | **N/A** — сняты в TB1 | **N/A** | **N/A** |

### E — Сеть после фона (опционально)

| Шаг | Действие | Ожидание | Прогон 1 | Прогон 2 |
|-----|----------|----------|----------|----------|
| E1 | Airplane mode до разблокировки → сеть → TMA | Тост ошибки resync или успешный bootstrap; без краша | | |

---

## 2. Критерии приёмки фазы 0

Все **A–C** — **PASS** на **двух** TMA-прогонах (разные ОС: iOS + Android предпочтительно). **D** = N/A.

| Критерий | Порог |
|----------|--------|
| Период / cash после unlock | Согласованы с API (`GET /api/game/bootstrap`) |
| Двойное закрытие месяца | **0** на прогон C |
| Потеря сессии | **0** re-login после unlock |
| BLOCKER по resume | **0** открытых после прогонов 1–2 |

**Итог фазы 0 (заполнить после TMA):** ⬜ PASS · ⬜ FAIL · ⬜ PASS с оговорками

---

## 3. Верификация API (опционально)

После A3 в DevTools / прокси (или desktop с тем же аккаунтом):

1. После unlock — `GET /api/game/bootstrap`.
2. `time.period_index` = hero **#N**; `overview.cash_balance` = KPI cash (± округление).

---

## 4. Решение после прогона

| Исход | Действие |
|-------|----------|
| **PASS** | PW1-004 [x] в бэклоге; вопрос §6.8 в Pre-Alpha; можно 0.3 |
| **FAIL** | BLOCKER: шаг, устройство, скрин; не расширять когорту |
| **PASS с оговорками** | UX-заметки; PWA фаза 1 — по плану |

---

## 5. Связь с Pre-Alpha

После PASS добавить в приглашение:

> Можно сворачивать Telegram или блокировать телефон — после возврата баланс и период подтянутся сами. Если цифры «застыли» — напишите в фидбек со временем.

Вопрос опроса: [PRE_ALPHA_PLAYTEST_PROTOCOL.md](PRE_ALPHA_PLAYTEST_PROTOCOL.md) §6, п. 8.

---

## A0. Legacy-сценарии с таймером (архив)

До TB1 использовались шаги «осталось N мин на таймере». В prod **не применять**; при возврате таймера в UI — восстановить из git history этого файла (2026-05-25).
