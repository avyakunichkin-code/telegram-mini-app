# Dashboard — page round: Section Header A

Цель: перед внедрением в prod увидеть **страницу Dashboard целиком** с новым унифицированным заголовком секций (**A**).

Важно:
- Тут утверждаем **только Section Header A** (kicker/title/subtitle + маленькая кнопка `?`).
- Стиль строк ниже — демонстрационный (underline + divider), чтобы понять ритм страницы.

## Запуск

```bash
cd design-lab/dashboard/section-header-a-page-round
.\sync-lab.ps1
npx serve .
```

## Утверждение

| Дата | Решение |
|------|---------|
| 2026-05-27 | A — без счётчика, `?` меньше |

