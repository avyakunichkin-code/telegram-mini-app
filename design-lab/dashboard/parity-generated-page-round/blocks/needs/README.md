# Z-NEEDS v7 — плоский блок

Концептуальный сдвиг от v5: **заголовок секции снаружи**, **4 шкалы без expand**. Prod: **e2e3** (книга+? + сердце-точка). Справка: `GET /api/game/needs/guide` — 4 раздела.

```bash
cd design-lab/character-needs/dashboard-needs-v7-round
./sync-lab.sh
npx serve .
```

Хаб: **Z-NEEDS v7 (плоский блок)**.

См. [`VARIANTS.md`](./VARIANTS.md).
