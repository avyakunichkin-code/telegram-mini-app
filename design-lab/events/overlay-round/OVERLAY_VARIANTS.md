# Раунд 3 — оверлей событий (L3 shell)

**Цель:** единый стиль окна с карточкой L3 — без длинного tagline, акцент домена на рамке/точках/стрелках.

## ★ O1 — L3 shell (prod)

| Слот | Решение |
|------|---------|
| Панель | Белый / едва заметный градиент |
| Карточка | L3: полоса домена → заголовок + × → лейблы → пузырь → flat-выборы |
| Подсказка | **Нет** |
| Навигация | Внизу: `1 / 2` + стрелки (без точек, без «События») |

## Запуск

```powershell
cd design-lab/events/overlay-round
.\sync-lab.sh
npx serve .
```

## Архив

Старые варианты B′ / M6 — [`../index.html`](../index.html).
