# Контент / a11y

- `aria-label` на progressbar: «Наполнение подушки N%»
- Подпись «×3» только в lab-hint; в prod chip label остаётся **Подушка**
- При `target === 0` полоску не показывать (деление на ноль)
