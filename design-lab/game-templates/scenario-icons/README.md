# Scenario icons (I-Scene) — архив

**Статус:** superseded · **2026-06-01**

Четыре набора **SVG**-иллюстраций для шага «Выбор персонажа». Заменены растровыми портретами в prod:

- Lab ★: [`../persona-portraits-round/`](../persona-portraits-round/)
- Код: `PersonaPortrait`, `personaPortraits.js`, `MqxStarterScenarioPicker` (`usePersonaPortraits` по умолчанию `true`)

## Когда смотреть этот раунд

- Сравнение стиля SVG vs портреты
- Fallback в коде: `ScenarioIllustrations.jsx` при `usePersonaPortraits={false}`

## Запуск

```bash
cd design-lab/game-templates/scenario-icons
npx serve .
```

Сводка вариантов: см. [`../README.md`](../README.md) (таблица `scenario-icons`).
