# Экраны (`screens/`)

Route- и flow-level UI. Сборка из `components/mqx/`, данные из `hooks/` и `api/`.

| Папка | Поток |
|-------|--------|
| [`pre-game/`](pre-game/) | Новая игра: режим → шаблоны Game |
| [`plan/`](plan/) | Plan mode (мастер) |
| `game/` | Игра в сессии (цель переноса `*Premium.jsx`) |

См. [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md).
