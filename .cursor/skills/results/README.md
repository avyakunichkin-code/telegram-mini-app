# Skill test results

Сюда пишутся отчёты `/skill-test spec [name]` (если пользователь согласовал запись).

Имя файла: `skill-test-spec-{skill-name}-{YYYY-MM-DD}.md`

Каталог обновляется в `.cursor/skills/catalog.yaml` (`last_spec`, `last_spec_result`).
